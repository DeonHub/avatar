<div className="flex h-screen"> {/* LEFT PANEL */}    <ImagePreview image={hoveredImage} /> {/* BOOK */} <div className="flex-1 relative bg-gray-100 flex items-center justify-center"> <BookPage images={pages[currentPage].images} onDropImage={(img, pos) => { setPages(prev => prev.map((p, i) => i === currentPage ? { ...p, images: [ ...p.images, { instanceId: crypto.randomUUID(), libraryId: img.id, src: img.src, ...pos, width: 120, height: 90, }, ], } : p ) ); }} onRemoveImage={removeFromBook} /> </div> </div> );